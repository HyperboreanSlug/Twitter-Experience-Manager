# Module: `core`

**Source:** `src/modules/core.js` · **Exports:** `Core`

Shared auth, GraphQL helpers, `localStorage` (`tem:*`), `fetchUserByScreenName`, and `blockUser` (REST `blocks/create.json`).

See also Tweepcred Manager’s core docs for the same dual-mode conventions (`@grant none`).
